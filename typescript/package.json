{
  "name": "code-similarity-analyzer",
  "version": "1.0.0",
  "description": "TypeScript implementation of code similarity analyzer",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node dist/test_similarity_analyzer.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["code", "similarity", "analysis", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "readline-sync": "^1.4.10"
  }
}
